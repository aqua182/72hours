# Make the LLM optional and schema-bounded

The AI integration will use an adapter that validates generated Evidence Claims against a schema and operates on Redacted Prompts. A configured API key enables a live LLM provider, while deterministic fixtures and seeded AI-scribed entries provide an equivalent offline demo path. This protects the evaluation from provider availability without relaxing provenance or privacy requirements.
