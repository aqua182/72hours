# AI signals must be source-linked and clinician-governed

Nightingale will treat AI output as candidate Evidence Claims linked to exact source spans, not as independent clinical facts. Only deterministic rules may create Risk Flags; those flags create cancellable review tasks, while clinician-confirmed content takes precedence over conflicting AI or patient material. This makes every Glance View signal inspectable and prevents model confidence or opaque scores from becoming clinical authority.
